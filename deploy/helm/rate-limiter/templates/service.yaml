apiVersion: v1
kind: Service
metadata:
  name: {{ include "rate-limiter.fullname" . }}
  labels:
    {{- include "rate-limiter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "rate-limiter.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.httpPort }}
      targetPort: http
    - name: grpc
      port: {{ .Values.service.grpcPort }}
      targetPort: grpc
