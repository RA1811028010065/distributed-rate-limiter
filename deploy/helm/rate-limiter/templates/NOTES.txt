1. Run `kubectl get pods -n {{ .Release.Namespace }}` to watch the rollout.
2. Port-forward the REST service with:
   `kubectl port-forward svc/{{ include "rate-limiter.fullname" . }} -n {{ .Release.Namespace }} 8080:{{ .Values.service.httpPort }}`
   then send a request:
   `curl -sS -X POST http://localhost:8080/api/v1/allow -H 'Content-Type: application/json' -d '{"key":"helm","tokens":1,"max_tokens":5,"refill_rate":5}'`
